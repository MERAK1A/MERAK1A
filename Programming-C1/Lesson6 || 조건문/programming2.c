#include <stdio.h>

int main(void)
{
    int score, num1;

    scanf("%d", &score);

    if (score > 100 || score < 0)
    {
        printf("error");
    }

    else
    {   
        if (score >= 95 && score <= 100)
        {
            num1 = 1;
        }
        
        else if (score >=)
        {

        }
        switch (num1)
        {
        case 1:
            /* code */
            break;
        
        default:
            break;
        }
    }
    
    
    return 0;
}